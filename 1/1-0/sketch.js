/*
* コメントアウトを使うと、プログラムとは全く関係のない文字を入力することができます。
* メモなどに使用してください。
*/

// １行コメントアウト
/*
    複数行コメントアウト
*/

function setup() {
    createCanvas(windowWidth, windowHeight);
    background(255);
}

function draw() {
    ellipse(width / 2, height / 2, 400, 400);
}
